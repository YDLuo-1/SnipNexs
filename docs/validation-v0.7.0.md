# SnipNexs v0.7.0 验证记录

- 日期：2026-08-24
- 系统：Windows 注册表报告 `Windows 10 Home China`，构建 `26200.9168`（该构建号对应当前 Windows 11 分支；本记录只保留本机实际返回值）
- Qt / 编译器：Qt 6.11.2 MSVC 2022 x64 / MSVC 19.44
- 显示缩放：未记录；本轮没有做混合 DPI 桌面验收
- 发布包校验：以同一 Release 附件 `SHA256SUMS.txt` 为准

## 自动验证

- Release 配置构建成功。
- CTest 共 13 项，零失败：11 项执行通过，`CaptureExclusionTests` 与 `NativeScreenRecorderTests` 因需要真实桌面组合器/录屏环境按设计跳过。
- 新增并通过：主题与链接对比度、应用图标生成、录屏悬浮条右下角定位、窗口目标排序与去重测试。

## 独立部署桌面验收

使用 `dist/SnipNexs-0.7.0-qa` 独立部署目录：

- 通过：首次启动仅进入托盘，不显示主窗口。
- 通过：再次运行同一路径可唤醒唯一主窗口。
- 通过：中英文主界面、关于页和开源许可页可切换并正常显示；普通文字、按钮和源码链接在深色背景上清晰可读。
- 通过：标题栏显示新的“截图取景框 + N”图标；关于页版本为 `0.7.0`。
- 通过：启动截图后主窗口从桌面窗口列表消失，没有覆盖截图内容。
- 未验证：全屏截图覆盖层不向桌面自动化工具暴露可操作窗口，本轮未自动点击不同子窗口来核对子 HWND / 客户区 / 整窗命中结果。
- 未验证：同一限制导致本轮未完成真实区域录屏、悬浮条右下角位置、拖动、停止、MP4 输出和捕获排除验证；这些功能只有自动逻辑测试证据。
- 未验证：100% / 150% / 200% 混合 DPI 历史切换。
- 通过：验收结束后没有残留 `SnipNexs.exe` 进程。

没有桌面证据的项目保持“未验证”，不计入通过项。
