# SnipNexs v0.7.1 验证记录

- 日期：2026-08-24
- 系统：Windows 本机，MSVC 2022 x64
- Qt / 编译器：Qt 6.11.2 MSVC 2022 x64 / MSVC 19.44
- 显示缩放：未记录；本轮没有做混合 DPI 桌面验收
- 发布包校验：以同一 Release 附件 `SHA256SUMS.txt` 为准

## 自动验证

- Release 配置构建成功。
- CTest 共 13 项，零失败：11 项执行通过，`CaptureExclusionTests` 与 `NativeScreenRecorderTests` 因需要真实桌面组合器/录屏环境按设计跳过。
- `AboutDialogTests` 通过：Qt 自带关于窗口的正文和链接对比度达标，关闭后应用调色板恢复。
- `CaptureOverlayTests` 通过：11 个工具栏按钮均为纯图标、包含 Tooltip/无障碍名称，贴图、复制和保存图标互不相同。
- `PinWindowTests` 通过：屏幕可用区域尺寸的大图初始贴图内容尺寸与原图逻辑尺寸一致，不再执行 80% 自动缩小。

## 桌面验收

- 未验证：本轮未操作用户桌面，尚未人工查看实际工具栏图标视觉效果。
- 未验证：尚未在真实 100% / 150% / 200% 显示缩放环境对比选区与贴图初始大小。
- 未验证：捕获排除、真实区域录屏和 MP4 输出。

没有桌面证据的项目保持“未验证”，不计入通过项。
